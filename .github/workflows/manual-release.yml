name: Manual Release


on:
  workflow_dispatch:
    inputs:
      version_type:
        description: 'Version type (patch, minor, major)'
        required: true
        default: 'patch'
        type: choice
        options:
        - patch
        - minor
        - major
      release_notes:
        description: 'Release notes'
        required: false
        type: string
      create_release:
        description: 'Create GitHub release'
        required: false
        default: true
        type: boolean

jobs:
  create-release:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.9"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install python-semantic-release

    - name: Get current version
      id: current_version
      run: |
        echo "current_version=$(cat VERSION)" >> $GITHUB_OUTPUT

    - name: Calculate new version
      id: new_version
      run: |
        current="${{ steps.current_version.outputs.current_version }}"
        version_type="${{ inputs.version_type }}"

        IFS='.' read -ra VERSION_PARTS <<< "$current"
        major=${VERSION_PARTS[0]}
        minor=${VERSION_PARTS[1]}
        patch=${VERSION_PARTS[2]}

        case $version_type in
          patch)
            patch=$((patch + 1))
            ;;
          minor)
            minor=$((minor + 1))
            patch=0
            ;;
          major)
            major=$((major + 1))
            minor=0
            patch=0
            ;;
        esac

        new_version="$major.$minor.$patch"
        echo "new_version=$new_version" >> $GITHUB_OUTPUT
        echo "New version: $new_version"

    - name: Update VERSION file
      run: |
        echo "${{ steps.new_version.outputs.new_version }}" > VERSION

    - name: Create git tag
      run: |
        git config --global user.name "github-actionsRun python -m build
* Creating isolated environment: venv+pip...
* Installing packages in isolated environment:
  - setuptools>=61.0
  - wheel
* Getting build dependencies for sdist...
/tmp/build-env-mnny305u/lib/python3.9/site-packages/setuptools/config/_apply_pyprojecttoml.py:82: SetuptoolsDeprecationWarning: `project.license` as a TOML table is deprecated
!!
        ********************************************************************************
        Please use a simple string containing a SPDX expression for `project.license`. You can also use `project.license-files`. (Both options available on setuptools>=77.0.0).
        By 2026-Feb-18, you need to update your project and remove deprecated calls
        or your builds will no longer be supported.
        See https://packaging.python.org/en/latest/guides/writing-pyproject-toml/#license for details.
        ********************************************************************************
!!
  corresp(dist, value, root_dir)
/tmp/build-env-mnny305u/lib/python3.9/site-packages/setuptools/config/_apply_pyprojecttoml.py:82: SetuptoolsWarning: `install_requires` overwritten in `pyproject.toml` (dependencies)
  corresp(dist, value, root_dir)
/tmp/build-env-mnny305u/lib/python3.9/site-packages/setuptools/config/_apply_pyprojecttoml.py:82: SetuptoolsWarning: `extras_require` overwritten in `pyproject.toml` (optional-dependencies)
  corresp(dist, value, root_dir)
/tmp/build-env-mnny305u/lib/python3.9/site-packages/setuptools/config/_apply_pyprojecttoml.py:61: SetuptoolsDeprecationWarning: License classifiers are deprecated.
!!
        ********************************************************************************
        Please consider removing the following classifiers in favor of a SPDX license expression:
        License :: OSI Approved :: MIT License
        See https://packaging.python.org/en/latest/guides/writing-pyproject-toml/#license for details.
        ********************************************************************************
!!
  dist._finalize_license_expression()
/tmp/build-env-mnny305u/lib/python3.9/site-packages/setuptools/dist.py:759: SetuptoolsDeprecationWarning: License classifiers are deprecated.
!!
        ********************************************************************************
        Please consider removing the following classifiers in favor of a SPDX license expression:
        License :: OSI Approved :: MIT License
        See https://packaging.python.org/en/latest/guides/writing-pyproject-toml/#license for details.
        ********************************************************************************
!!
  self._finalize_license_expression()
running egg_info
creating bash_script_maker.egg-info
writing bash_script_maker.egg-info/PKG-INFO
writing dependency_links to bash_script_maker.egg-info/dependency_links.txt
writing entry points to bash_script_maker.egg-info/entry_points.txt
writing requirements to bash_script_maker.egg-info/requires.txt
writing top-level names to bash_script_maker.egg-info/top_level.txt
writing manifest file 'bash_script_maker.egg-info/SOURCES.txt'
error: package directory 'bash_script_maker' does not exist
ERROR Backend subprocess exited when trying to invoke get_requires_for_build_sdist
Error: Process completed with exit code 1.ps.new_version.outputs.new_version }}"
        git tag "v${{ steps.new_version.outputs.new_version }}"

    - name: Push changes and tags
      run: |
        git push origin main
        git push origin "v${{ steps.new_version.outputs.new_version }}"

    - name: Create GitHub Release
      if: ${{ inputs.create_release }}
      uses: actions/create-release@v1.2.0
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: "v${{ steps.new_version.outputs.new_version }}"
        release_name: "Release ${{ steps.new_version.outputs.new_version }}"
        body: |
          ## What's Changed

          ${{ inputs.release_notes || 'Automated release' }}

          ### Version Information
          - **Version**: ${{ steps.new_version.outputs.new_version }}
          - **Previous Version**: ${{ steps.current_version.outputs.current_version }}
          - **Version Type**: ${{ inputs.version_type }}

          ### Installation
          ```bash
          pip install bash-script-maker==${{ steps.new_version.outputs.new_version }}
          ```

          ### Downloads
          - Source code: [ZIP](https://github.com/${{ github.repository }}/archive/v${{ steps.new_version.outputs.new_version }}.zip)
          - Tarball: [TAR.GZ](https://github.com/${{ github.repository }}/archive/v${{ steps.new_version.outputs.new_version }}.tar.gz)
        draft: false
        prerelease: false
# Finish