name: "CodeQL Configuration"

disable-default-queries: false

queries:
  - uses: security-and-quality
  - uses: security-experimental

paths-ignore:
  - __pycache__
  - "*.pyc"
  - ".pytest_cache"
  - "docs"
  - "tests"  # Wenn später Tests hinzugefügt werden
