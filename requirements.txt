# Core dependencies
# tkinter wird über System-Paketmanager installiert (python3-tk)

# Development dependencies (optional)
pytest>=6.0.0
pytest-cov>=2.10.0
flake8>=3.8.0
black>=21.0.0
mypy>=0.800
tox>=3.20.0
twine>=3.0.0
build>=0.7.0
bandit>=1.7.0
# Pin wegen Typer-Inkompatibilitäten in CI
safety==2.3.5

# Documentation
pdoc3>=0.9.0

# Pre-commit (for local development)
pre-commit>=2.15.0

# GUI
ttkbootstrap>=1.10.1
